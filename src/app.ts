import "./Utils/Bootstrap";

import Bootstrap from "./Bootstrap";

const app = new Bootstrap();
app.listen();

console.log("Log is working!");
console.info("Info is working!");
console.debug("Debug is working!");
console.warn("Warn is working!");
console.error("Error is working!");
